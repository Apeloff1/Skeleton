# Archived planning snapshots

These files are retained for project history, but they are not current implementation contracts or operational instructions.

Use the root `README.md`, `RUNBOOK.md`, active architecture documents, tests, and current pull requests for the repository's live behavior and integration state. Historical planning snapshots are kept here so useful context remains searchable without crowding the repository root.
