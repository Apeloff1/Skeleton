# Cue fold (GB-47)

Parent #80. Additive. Does not edit `Cue`.

## Public card

- Tokens join with `.`.
- A spaced token is dropped and not stored.

## Accept

`python -m unittest tests.test_gb47_fold -v`

`python scripts/check_fold.py` exits 0.
