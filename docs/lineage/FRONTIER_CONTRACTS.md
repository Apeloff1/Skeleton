# Frontier contracts

The frontier runtime uses small, validated, provider-neutral contracts. New components must preserve bounded resource usage, deterministic behavior, observable outcomes, and fail-closed admission.
