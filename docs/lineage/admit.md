# Era admit (GB-48)

Parent #80. Additive. Does not edit `EraBind` or `GameForgeRun`.

## Public card

- A citation token is admitted.
- Spaced text is refused and not stored.

## Accept

`python -m unittest tests.test_gb48_admit -v`

`python scripts/check_admit.py` exits 0.
