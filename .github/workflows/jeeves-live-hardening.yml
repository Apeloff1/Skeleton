name: Jeeves Live Hardening

on:
  pull_request:
    branches: [main]
    paths:
      - "skeleton/jeeves/llm_core.py"
      - "skeleton/jeeves/providers.py"
      - "tests/test_jeeves_live_hardening.py"
      - "tests/test_jeeves_adversarial.py"
      - "tests/test_jeeves_adversarial_followup.py"
      - "tests/test_jeeves_history_boundary.py"
      - "tests/test_jeeves_local_provider_policy.py"
      - ".github/workflows/jeeves-live-hardening.yml"
  push:
    branches: [main]
    paths:
      - "skeleton/jeeves/llm_core.py"
      - "skeleton/jeeves/providers.py"
      - "tests/test_jeeves_live_hardening.py"
      - "tests/test_jeeves_adversarial.py"
      - "tests/test_jeeves_adversarial_followup.py"
      - "tests/test_jeeves_history_boundary.py"
      - "tests/test_jeeves_local_provider_policy.py"
      - ".github/workflows/jeeves-live-hardening.yml"

permissions:
  contents: read

concurrency:
  group: jeeves-live-hardening-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  focused-regression:
    name: Provider and capability regressions
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
      - name: Install focused runtime slice
        run: python -m pip install "pytest>=8,<9" "pydantic>=2.5,<3" "pydantic-settings>=2.1,<3"
      - name: Compile Jeeves boundary
        run: python -m compileall -q skeleton/jeeves
      - name: Run focused regressions
        env:
          PYTHONPATH: "."
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
        run: >-
          python -m pytest -q
          tests/test_jeeves_live_hardening.py
          tests/test_jeeves_adversarial.py
          tests/test_jeeves_adversarial_followup.py
          tests/test_jeeves_history_boundary.py
          tests/test_jeeves_local_provider_policy.py
