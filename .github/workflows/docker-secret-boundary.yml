name: Docker build-context secret boundary

on:
  pull_request:
    paths:
      - 'Dockerfile'
      - '.dockerignore'
      - 'backend/Dockerfile'
      - 'frontend/Dockerfile'
      - 'frontend/.dockerignore'
      - 'backend/scripts/check_docker_secret_boundary.py'
      - 'backend/tests/test_docker_secret_boundary.py'
      - '.github/workflows/docker-secret-boundary.yml'
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - '.dockerignore'
      - 'backend/Dockerfile'
      - 'frontend/Dockerfile'
      - 'frontend/.dockerignore'
      - 'backend/scripts/check_docker_secret_boundary.py'
      - 'backend/tests/test_docker_secret_boundary.py'
      - '.github/workflows/docker-secret-boundary.yml'
  workflow_dispatch:

concurrency:
  group: docker-secret-boundary-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  boundary:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install focused test dependency
        run: python -m pip install --disable-pip-version-check 'pytest>=8,<9'
      - name: Validate Docker build-context and build-layer secret boundary
        run: python backend/scripts/check_docker_secret_boundary.py
      - name: Regression tests
        env:
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: '1'
        run: python -m pytest -q --noconftest backend/tests/test_docker_secret_boundary.py
