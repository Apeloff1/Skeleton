name: Merge Readiness

on:
  push:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  syntax_probe:
    name: Syntax Probe
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow parses
        run: echo "merge-readiness workflow parsed"
