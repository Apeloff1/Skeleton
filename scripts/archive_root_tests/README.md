# Archive lane — root tests

GB-8 Track E. Root `*_test.py` and `test_*.py` files move here or to
`tests/legacy_root/`. Do not delete. Do not park `SEVEN_BY_*.md` here
(that is GB-8b → `docs/archive/seven_by/`).

Canonical live archive of the September 2026 hygiene pass lives in
`tests/legacy_root/`. This directory is the alternate lane named by the
artifact-plane skill for scripts that are not pytest modules.
