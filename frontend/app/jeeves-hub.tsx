/**
 * Backward-compatible route for the Jeeves Powerhouse Hub.
 *
 * The operator console is the evolved hub surface: it keeps the same service
 * fleet while adding latency, health classification, focused retries and
 * operational quick actions. Keeping this alias preserves existing deep links.
 */
export { default } from './jeeves-ops';
