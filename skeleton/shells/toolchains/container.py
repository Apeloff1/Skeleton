"""Container runtime logical command contracts."""

from __future__ import annotations

from skeleton.shells.toolchains.factory import OperationSpec, OptionSpec, build_many
from skeleton.shells.toolchains.types import CommandEffect, CommandRisk

SPECS = (
    OperationSpec(
        logical_name="container.docker_version",
        executable_key="docker",
        subcommand="--version",
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_version",
    ),
    OperationSpec(
        logical_name="container.docker_info",
        executable_key="docker",
        subcommand="info",
        options=(OptionSpec("--format", "safe"),),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=120.0,
        tags=frozenset(("container",)),
        description="container.docker_info",
    ),
    OperationSpec(
        logical_name="container.docker_ps",
        executable_key="docker",
        subcommand="ps",
        options=(OptionSpec("--all"), OptionSpec("--quiet"), OptionSpec("--filter", "safe", True), OptionSpec("--format", "safe"), OptionSpec("--no-trunc")),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_ps",
    ),
    OperationSpec(
        logical_name="container.docker_images",
        executable_key="docker",
        subcommand="images",
        options=(OptionSpec("--all"), OptionSpec("--quiet"), OptionSpec("--filter", "safe", True), OptionSpec("--format", "safe"), OptionSpec("--digests")),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_images",
    ),
    OperationSpec(
        logical_name="container.docker_inspect",
        executable_key="docker",
        subcommand="inspect",
        options=(OptionSpec("--format", "safe"), OptionSpec("--type", "short")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_inspect",
    ),
    OperationSpec(
        logical_name="container.docker_logs",
        executable_key="docker",
        subcommand="logs",
        options=(OptionSpec("--tail", "integer"), OptionSpec("--timestamps"), OptionSpec("--since", "short"), OptionSpec("--until", "short")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_logs",
    ),
    OperationSpec(
        logical_name="container.docker_top",
        executable_key="docker",
        subcommand="top",
        positionals=("identifier",),
        variadic="short",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_top",
    ),
    OperationSpec(
        logical_name="container.docker_stats_once",
        executable_key="docker",
        subcommand="stats",
        options=(OptionSpec("--no-stream"), OptionSpec("--no-trunc"), OptionSpec("--format", "safe")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_stats_once",
    ),
    OperationSpec(
        logical_name="container.docker_history",
        executable_key="docker",
        subcommand="history",
        options=(OptionSpec("--no-trunc"), OptionSpec("--quiet"), OptionSpec("--format", "safe")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_history",
    ),
    OperationSpec(
        logical_name="container.docker_image_inspect",
        executable_key="docker",
        subcommand="image",
        positionals=("short", "identifier"),
        min_positionals=3,
        max_positionals=3,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_image_inspect",
    ),
    OperationSpec(
        logical_name="container.docker_build",
        executable_key="docker",
        subcommand="build",
        options=(OptionSpec("--file", "path"), OptionSpec("--tag", "short", True), OptionSpec("--build-arg", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--pull"), OptionSpec("--no-cache"), OptionSpec("--target", "short")),
        positionals=("path",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.BUILD, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=1800.0,
        tags=frozenset(("container",)),
        description="container.docker_build",
    ),
    OperationSpec(
        logical_name="container.docker_pull",
        executable_key="docker",
        subcommand="pull",
        options=(OptionSpec("--quiet"), OptionSpec("--platform", "short")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.NETWORK, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=900.0,
        tags=frozenset(("container",)),
        description="container.docker_pull",
    ),
    OperationSpec(
        logical_name="container.docker_create",
        executable_key="docker",
        subcommand="create",
        options=(OptionSpec("--name", "identifier"), OptionSpec("--env", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--entrypoint", "path")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_create",
    ),
    OperationSpec(
        logical_name="container.docker_run",
        executable_key="docker",
        subcommand="run",
        options=(OptionSpec("--rm"), OptionSpec("--name", "identifier"), OptionSpec("--env", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--read-only"), OptionSpec("--network", "short"), OptionSpec("--cpus", "short"), OptionSpec("--memory", "short")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=1800.0,
        tags=frozenset(("container",)),
        description="container.docker_run",
    ),
    OperationSpec(
        logical_name="container.docker_start",
        executable_key="docker",
        subcommand="start",
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_start",
    ),
    OperationSpec(
        logical_name="container.docker_stop",
        executable_key="docker",
        subcommand="stop",
        options=(OptionSpec("--time", "integer"),),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=300.0,
        tags=frozenset(("container",)),
        description="container.docker_stop",
    ),
    OperationSpec(
        logical_name="container.docker_kill",
        executable_key="docker",
        subcommand="kill",
        options=(OptionSpec("--signal", "short"),),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_kill",
    ),
    OperationSpec(
        logical_name="container.docker_rm",
        executable_key="docker",
        subcommand="rm",
        options=(OptionSpec("--force"), OptionSpec("--volumes")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_rm",
    ),
    OperationSpec(
        logical_name="container.docker_rmi",
        executable_key="docker",
        subcommand="rmi",
        options=(OptionSpec("--force"), OptionSpec("--no-prune")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_rmi",
    ),
    OperationSpec(
        logical_name="container.docker_cp",
        executable_key="docker",
        subcommand="cp",
        positionals=("safe", "safe"),
        min_positionals=3,
        max_positionals=3,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=600.0,
        tags=frozenset(("container",)),
        description="container.docker_cp",
    ),
    OperationSpec(
        logical_name="container.docker_exec",
        executable_key="docker",
        subcommand="exec",
        options=(OptionSpec("--env", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--user", "short")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS}),
        risk=CommandRisk.HIGH,
        timeout=900.0,
        tags=frozenset(("container",)),
        description="container.docker_exec",
    ),
    OperationSpec(
        logical_name="container.docker_network_ls",
        executable_key="docker",
        subcommand="network",
        positionals=("literal:ls",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_network_ls",
    ),
    OperationSpec(
        logical_name="container.docker_volume_ls",
        executable_key="docker",
        subcommand="volume",
        positionals=("literal:ls",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_volume_ls",
    ),
    OperationSpec(
        logical_name="container.docker_system_df",
        executable_key="docker",
        subcommand="system",
        positionals=("literal:df",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.docker_system_df",
    ),
    OperationSpec(
        logical_name="container.podman_version",
        executable_key="podman",
        subcommand="--version",
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_version",
    ),
    OperationSpec(
        logical_name="container.podman_info",
        executable_key="podman",
        subcommand="info",
        options=(OptionSpec("--format", "safe"),),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=120.0,
        tags=frozenset(("container",)),
        description="container.podman_info",
    ),
    OperationSpec(
        logical_name="container.podman_ps",
        executable_key="podman",
        subcommand="ps",
        options=(OptionSpec("--all"), OptionSpec("--quiet"), OptionSpec("--filter", "safe", True), OptionSpec("--format", "safe"), OptionSpec("--no-trunc")),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_ps",
    ),
    OperationSpec(
        logical_name="container.podman_images",
        executable_key="podman",
        subcommand="images",
        options=(OptionSpec("--all"), OptionSpec("--quiet"), OptionSpec("--filter", "safe", True), OptionSpec("--format", "safe"), OptionSpec("--digests")),
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_images",
    ),
    OperationSpec(
        logical_name="container.podman_inspect",
        executable_key="podman",
        subcommand="inspect",
        options=(OptionSpec("--format", "safe"), OptionSpec("--type", "short")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_inspect",
    ),
    OperationSpec(
        logical_name="container.podman_logs",
        executable_key="podman",
        subcommand="logs",
        options=(OptionSpec("--tail", "integer"), OptionSpec("--timestamps"), OptionSpec("--since", "short"), OptionSpec("--until", "short")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_logs",
    ),
    OperationSpec(
        logical_name="container.podman_top",
        executable_key="podman",
        subcommand="top",
        positionals=("identifier",),
        variadic="short",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_top",
    ),
    OperationSpec(
        logical_name="container.podman_stats_once",
        executable_key="podman",
        subcommand="stats",
        options=(OptionSpec("--no-stream"), OptionSpec("--no-trunc"), OptionSpec("--format", "safe")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_stats_once",
    ),
    OperationSpec(
        logical_name="container.podman_history",
        executable_key="podman",
        subcommand="history",
        options=(OptionSpec("--no-trunc"), OptionSpec("--quiet"), OptionSpec("--format", "safe")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_history",
    ),
    OperationSpec(
        logical_name="container.podman_image_inspect",
        executable_key="podman",
        subcommand="image",
        positionals=("short", "identifier"),
        min_positionals=3,
        max_positionals=3,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_image_inspect",
    ),
    OperationSpec(
        logical_name="container.podman_build",
        executable_key="podman",
        subcommand="build",
        options=(OptionSpec("--file", "path"), OptionSpec("--tag", "short", True), OptionSpec("--build-arg", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--pull"), OptionSpec("--no-cache"), OptionSpec("--target", "short")),
        positionals=("path",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.BUILD, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=1800.0,
        tags=frozenset(("container",)),
        description="container.podman_build",
    ),
    OperationSpec(
        logical_name="container.podman_pull",
        executable_key="podman",
        subcommand="pull",
        options=(OptionSpec("--quiet"), OptionSpec("--platform", "short")),
        positionals=("identifier",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.NETWORK, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=900.0,
        tags=frozenset(("container",)),
        description="container.podman_pull",
    ),
    OperationSpec(
        logical_name="container.podman_create",
        executable_key="podman",
        subcommand="create",
        options=(OptionSpec("--name", "identifier"), OptionSpec("--env", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--entrypoint", "path")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_create",
    ),
    OperationSpec(
        logical_name="container.podman_run",
        executable_key="podman",
        subcommand="run",
        options=(OptionSpec("--rm"), OptionSpec("--name", "identifier"), OptionSpec("--env", "safe", True), OptionSpec("--label", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--read-only"), OptionSpec("--network", "short"), OptionSpec("--cpus", "short"), OptionSpec("--memory", "short")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=1800.0,
        tags=frozenset(("container",)),
        description="container.podman_run",
    ),
    OperationSpec(
        logical_name="container.podman_start",
        executable_key="podman",
        subcommand="start",
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_start",
    ),
    OperationSpec(
        logical_name="container.podman_stop",
        executable_key="podman",
        subcommand="stop",
        options=(OptionSpec("--time", "integer"),),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.MODERATE,
        timeout=300.0,
        tags=frozenset(("container",)),
        description="container.podman_stop",
    ),
    OperationSpec(
        logical_name="container.podman_kill",
        executable_key="podman",
        subcommand="kill",
        options=(OptionSpec("--signal", "short"),),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS, CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_kill",
    ),
    OperationSpec(
        logical_name="container.podman_rm",
        executable_key="podman",
        subcommand="rm",
        options=(OptionSpec("--force"), OptionSpec("--volumes")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_rm",
    ),
    OperationSpec(
        logical_name="container.podman_rmi",
        executable_key="podman",
        subcommand="rmi",
        options=(OptionSpec("--force"), OptionSpec("--no-prune")),
        variadic="identifier",
        min_positionals=1,
        max_positionals=None,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_rmi",
    ),
    OperationSpec(
        logical_name="container.podman_cp",
        executable_key="podman",
        subcommand="cp",
        positionals=("safe", "safe"),
        min_positionals=3,
        max_positionals=3,
        effects=frozenset({CommandEffect.WRITE}),
        risk=CommandRisk.HIGH,
        timeout=600.0,
        tags=frozenset(("container",)),
        description="container.podman_cp",
    ),
    OperationSpec(
        logical_name="container.podman_exec",
        executable_key="podman",
        subcommand="exec",
        options=(OptionSpec("--env", "safe", True), OptionSpec("--workdir", "path"), OptionSpec("--user", "short")),
        positionals=("identifier",),
        variadic="safe",
        min_positionals=2,
        max_positionals=None,
        effects=frozenset({CommandEffect.PROCESS}),
        risk=CommandRisk.HIGH,
        timeout=900.0,
        tags=frozenset(("container",)),
        description="container.podman_exec",
    ),
    OperationSpec(
        logical_name="container.podman_network_ls",
        executable_key="podman",
        subcommand="network",
        positionals=("literal:ls",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_network_ls",
    ),
    OperationSpec(
        logical_name="container.podman_volume_ls",
        executable_key="podman",
        subcommand="volume",
        positionals=("literal:ls",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_volume_ls",
    ),
    OperationSpec(
        logical_name="container.podman_system_df",
        executable_key="podman",
        subcommand="system",
        positionals=("literal:df",),
        min_positionals=2,
        max_positionals=2,
        effects=frozenset({CommandEffect.READ}),
        risk=CommandRisk.LOW,
        timeout=60.0,
        tags=frozenset(("container",)),
        description="container.podman_system_df",
    ),
)

CONTRACTS = build_many(SPECS)


def contracts():
    return CONTRACTS
