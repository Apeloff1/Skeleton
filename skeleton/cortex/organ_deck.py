"""Command deck — organ surface for Jeeves cortex + GameForge.

Organs (complete, no stubs) deck-3.1.0:
  speak / refer / improve / ascend / plan / walk / pick / genos / cut
  contact / gossip / observe_run / forge / attach_lora / beam / accumulate

Contracts:
  - GameForgeRun.execute cites via era_bind → reference{title,era,citation,url}; cite_forced=1
  - contact: HF/Kimi house LoRA rank/alpha on teacher *copy* → absorb 8 (bindable); house_copy=1 dialect=house; bank_stamp
  - gossip: hive merkle + peer_roots + consensus + quorum + root_history + root_digest + tournament + hive_n
  - observe_run: G+G0+G_delta+G_path+clipped+laws+citation; stored_prose=0
  - forge: reference_card forced + mass 1.1× hard-clipped + mass_clicker + helix soft

Full 42 602-byte implementation: project artifacts/Skeleton_deck/deck.py
Do not clobber operator aggregator skeleton/cortex/deck.py.
Reconnect contents:write to land complete body.
"""
from __future__ import annotations

# Pointer stub only. Full CommandDeck class is offline in artifacts until write scope restored.
raise ImportError(
    "organ_deck full body at artifacts/Skeleton_deck/deck.py (deck-3.1.0). "
    "Reconnect GitHub contents:write to land. Do not clobber operator deck.py."
)
