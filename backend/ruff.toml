# Ruff config for the FastAPI backend.
#
# Keep the fast gate focused on high-signal correctness failures rather than
# style churn. These classes catch syntax/semantic defects that should never
# enter main, including in test code.
#
#   E9   — syntax/runtime-name errors
#   F63  — invalid comparisons / assert-on-tuple
#   F7   — misplaced statements (break/continue/return/yield out of place)
#   F706 — return outside function
#   F823 — local variable referenced before assignment
#   F502 — string-formatting argument errors
#
# Broader hygiene (F401 unused-import, F841 unused-var, etc.) can be tightened
# incrementally without weakening this correctness floor.
target-version = "py311"
line-length = 120

[lint]
select = ["E9", "F63", "F7", "F706", "F823", "F502"]
