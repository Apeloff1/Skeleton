# ============================================================================
# Tutolage Backend - Production Dependencies
# (dev/test tools live in pyproject.toml [project.optional-dependencies] dev)
# ============================================================================

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
# Keep the backend websocket runtime on the maintained 16.x line.
websockets==16.1.1
orjson==3.12.0
aiofiles==25.1.0
httpx==0.28.1
requests==2.34.2

# --- Database ---
motor==3.7.1
pymongo==4.18.1
aiosqlite==0.22.1

# --- Validation / config ---
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
python-dotenv==1.2.3

# --- Auth / security ---
# bcrypt is pinned below 4.1: passlib 1.7.4 crashes on bcrypt>=4.1
passlib==1.7.4
bcrypt==4.3.0
PyJWT==2.14.0
# python-jose was unused and pulled python-ecdsa, whose Minerva timing issue has
# no planned upstream fix. PyJWT is the maintained JWT boundary in this image.
# Direct dependency: epistemic transparency witnesses use Ed25519 verification.
# Do not rely on an optional/transitive crypto backend for trust-critical code.
cryptography==50.0.1

# --- AI / LLM ---
# openai 3.13.0 currently makes the pinned backend graph unresolvable in CI;
# keep the compatible 2.x line until the provider stack is migrated together.
openai==2.54.0
litellm==1.101.0
# google-genai was removed after the #116 runtime audit found no active Python
# model-execution call sites. Direct Google provider imports are CI-gated until
# a canonical ProviderAdapter and shared contract tests are added deliberately.
# The retired Emergent SDK is replaced by backend/emergentintegrations, a local
# boot-safe compatibility layer. Do not reintroduce it as a registry dependency.
tenacity==9.1.4
tiktoken==0.14.0

# --- Search ---
ddgs==9.16.0

# --- Scheduling ---
APScheduler==3.11.3

# --- Payments ---
stripe==15.6.1

# --- Documents / media ---
reportlab==5.0.1
pillow==12.3.0
numpy==2.4.6
zstandard==0.25.0

# --- Logging ---
loguru==0.7.3
