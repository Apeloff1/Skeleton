# Tutolage backend

Package name `tutolage`. The organism lives in `skeleton/` at the repo root.
See the root [README.md](../README.md) for GameForge CLI, cortex LM, and the
Mishima Zaibatsu tournament (`python -m skeleton zaibatsu`).
